{
  "name": "teeny-url-api",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "dotenv -e .env nodemon server.js",
    "test": "vitest --no-threads --no-isolate",
    "coverage": "vitest run --coverage --no-threads --no-isolate",
    "test:ui": "vitest --ui --no-threads --no-isolate",
    "prettier": "prettier --write \"{src,tests}/**/*.js\" --config \".prettierrc\""
  },
  "dependencies": {
    "@faker-js/faker": "^7.6.0",
    "cors": "^2.8.5",
    "debug": "^4.3.4",
    "express": "^4.18.2",
    "helmet": "^6.0.0",
    "mongoose": "^6.7.2",
    "zod": "^3.19.1"
  },
  "devDependencies": {
    "@vitest/coverage-c8": "^0.25.2",
    "@vitest/ui": "^0.25.2",
    "dotenv-cli": "^6.0.0",
    "nodemon": "^2.0.20",
    "prettier": "^2.8.0",
    "supertest": "^6.3.1",
    "vitest": "^0.25.2"
  }
}
